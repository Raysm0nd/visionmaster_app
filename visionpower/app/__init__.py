"""PySide6 desktop UI (optional ``gui`` extra).

This package depends on Qt (PySide6) and draws its own node canvas. The engine
in ``visionpower.core`` does not depend on anything here — the dependency only
points one way.
"""
