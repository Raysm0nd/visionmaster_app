"""PySide6 desktop UI (optional ``gui`` extra).

This package depends on Qt and NodeGraphQt. The engine in ``visionpower.core``
does not depend on anything here — the dependency only points one way.
"""
